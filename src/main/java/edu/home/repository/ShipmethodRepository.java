package edu.home.repository;

import edu.home.entity.Shipmethod;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ShipmethodRepository extends JpaRepository<Shipmethod, Long> {
}